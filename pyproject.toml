[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "revue"
version = "1.0.0"
requires-python = ">=3.11"

[project.scripts]
revue = "main:main"

[tool.setuptools]
py-modules = ["main"]
